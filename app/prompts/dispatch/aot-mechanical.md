Client: aot-mechanical (slug). This is a POST-LAUNCH QUALITY PASS on a site that is already live and already emailed to the real business (info@aotmec.com) — https://aot-mechanical.grayreserve.agency. Do not touch anything under /find, /gather, deploy tagging, or GHL — none of that is in scope. This is purely: fix real, verified defects in the built site, rebuild, redeploy the same client (deploy will just re-push the same Vercel project/alias, it will NOT re-tag or re-enroll the contact — the demo-built tag and GHL contact already exist from the first deploy and must not be touched again).

The operator (Jeff) reviewed the live site personally and found several real problems. Each one below has already been independently verified in this session — do not re-litigate whether they're real, they are. Fix them.

## Already fixed by hand this session — DO NOT REDO, verify and build on top
- clients/aot-mechanical/site/src/app/services/cooling/page.tsx, /heating/page.tsx, /ductwork/page.tsx — 3 new dedicated service pages written (previously only backup-generators existed). Mirror the existing backup-generators/page.tsx structure.
- clients/aot-mechanical/site/src/app/_components/site-data.ts — SERVICE_CATEGORIES all 4 entries now have dedicated:true, routeLabels and ROUTES updated for the 3 new pages.
- clients/aot-mechanical/site/src/app/_components/SiteNav.tsx — fully rewritten with a real dropdown: desktop hover panel (chevron + onMouseEnter/onMouseLeave + onFocus), mobile expandable inline group, driven by SERVICE_CATEGORIES, list format (not an icon-card grid — that pattern is banned per build/SKILL.md's four-skill design consult). Verify this compiles and renders correctly; refine styling if needed but keep the list-not-grid structure.
- scripts/dispatch-build.sh — the stall/ceiling watchdog was redesigned (Fable consult) to distinguish CPU-active "composing" from truly dead processes, and to extend the ceiling instead of blind-killing an actively-writing run. Not your concern for this dispatch, just context for why this run should behave better than the last three.

## Verified defects to fix — evidence already gathered, act on it directly

**1. Hero has zero motion — root cause confirmed.** `page.tsx` calls `<HeroVideo poster="/images/gmaps1.webp" alt="..." />` with NO `src` prop. Read `_components/HeroVideo.tsx`'s own doc comment: with no `src` it deliberately degrades to a static poster image — by design, not a bug in that component. The actual gap is that the Remotion hero-video render step (`services/hero-video/`, proven working per this project's VID-01 ledger record — measured 4 photos -> 8.3s 720p clip) was never run for this client. Render a real hero video from the 4 vision-gated photos already in `clients/aot-mechanical/data/images/` (gmaps1.jpg, gmaps3.jpg, gmaps4.jpg, gmaps6.jpg), output it per the established pattern (hero.mp4 + poster), and pass BOTH `poster` and `src` to `<HeroVideo>`. If Remotion rendering is not feasible in this dispatch's time budget, that is an acceptable finding to report back — but do not silently leave the hero static without explaining why in status.md; try it first.

**2. Sections are visually flat — measured, not just perception.** `richness-check.mjs` already flagged this build with "1 photo-grounded section vs recommended 2+" and only 4 sRGB gradients total. Direct inspection of `page.tsx` confirms: of ~7 major sections, only 2 use `grain-dark`/`glow` texture classes; the rest are bare `bg-surface`/`bg-surface-2`/`bg-surface-6` with nothing else — no gradients, no subtle photo overlays, no texture. Add genuine visual richness to at least 2 more sections: a subtle background gradient (using the existing surface-ladder tokens, not new colors), a low-opacity (~15-20%) photo overlay grounding a section in a real image (per the "photo-grounded section" richness-check terminology), or textural treatment consistent with what's already used elsewhere on this site. Re-run richness-check.mjs after and confirm the photo-grounded-section count and gradient count both improve — report the before/after numbers in status.md, don't just claim it's fixed.

**3. HyperUI citations are decorative, not load-bearing — measured.** `qa-report.md` from the prior build already recorded: `HYPERUI_TRANSPLANT_CHECK=WARN (8 DECORATIVE citations, no FAIL lines)`. Read `scripts/hyperui-transplant-check.mjs` (or equivalent) to understand exactly what "decorative citation" means in this codebase's own terms, then go through each `data-hyperui="..."` tag in this site's pages and verify the actual markup genuinely reflects that HyperUI component's distinctive structure/styling — not just a generic div wearing the attribute as a label. Fix any that are decorative-only. Re-run the check after and confirm the WARN count drops, report the number in status.md.

**4. Services page photo cards render with a distracting solid teal-green cast — a real bug, not intentional-but-strong.** `.photo-duotone::after` applies `background: var(--secondary); mix-blend-mode: color; opacity: 0.7`, and this build's `--secondary` is `#005A4E` (teal). At 70% opacity with `mix-blend-mode: color` over a grayscale photo, this reads as a solid green wash, not a subtle brand tint — the operator's own reaction on seeing it live was "the services images are green?", i.e. it reads as broken. Reduce the opacity substantially (try 0.12-0.18) so it reads as a tint, not a color cast. This CSS is likely in `globals.css` — grep for `photo-duotone`.

**5. Contact page has no actual form — every other real client in this codebase has one, this build is the outlier.** `clients/aot-mechanical/site/src/app/contact/page.tsx` only has a static info/map section with `mailto:` links, no `<form>`. Compare `clients/abacus-plumbing/site/src/app/_components/ContactForm.tsx` and `clients/demolition-okc/site/src/app/_components/ContactForm.tsx` — both are real client components posting to `/api/preview/{slug}/contact` (the shared-lane previews app endpoint; aot-mechanical is deployed on the same shared-lane pattern so the same endpoint applies). Add an equivalent ContactForm component to aot-mechanical and wire it into the contact page, matching this site's own design system (Bitter/Epilogue, brand red #E00000, Exaggerated Minimalism) rather than copying the other client's visual treatment verbatim.

**6. CTA buttons are generic — real comparison already done.** AOT's buttons use `rounded-lg` with `px-6 py-3.5` and only `hover:opacity-90` (no press/active feedback). Compare `abacus-plumbing/site/src/app/page.tsx`: `rounded-full` pill buttons with `px-8/px-10 py-4/py-3`, `font-display font-bold`. AOT's own documented design direction is "Exaggerated Minimalism" (bold, high-contrast, statement design) — the current `rounded-lg` treatment doesn't commit to that. Give the primary CTA buttons across all pages (home, services, all 4 service subpages) a more deliberate, distinctive shape consistent with Exaggerated Minimalism (consider sharper/near-square corners rather than a generic medium radius, since soft pill shapes belong to a different aesthetic than this site's own), AND add real press feedback per this project's own ui-ux-pro-max rule (`active:scale-[0.97]` or `active:-translate-y-[1px]` — apply consistently, not just on the ones I already touched in SiteNav.tsx).

**7. FAQ section on the homepage stacks vertically instead of using 2 columns.** `page.tsx`'s FAQ section: `<dl ... className="mt-12 space-y-4">` wrapping `faq.map(...)`. Change to a responsive 2-column grid on larger screens while keeping mobile stacked: something like `mt-12 grid gap-4 sm:grid-cols-2`.

## Process
Fix all 7 in one pass. After fixing, run this site's full local build + the deterministic gates (contrast-check, richness-check, ship-scan, hyperui-transplant-check, token-check — whichever this build/SKILL.md's Verify section specifies) before touching deploy. Do NOT skip verification to save time — this exact site already went out to a real business once; a second visibly broken deploy is worse than the wait.

Once verified clean, redeploy aot-mechanical (same client, same Vercel project/alias — this is a re-push, not a new claim/tag/enrollment). Confirm the live URL serves the fixed version (check a couple of the specific fixes directly — the FAQ grid, the reduced duotone opacity, the hero video if rendered, at least one new service page + the nav dropdown).

Write a clear, itemized summary in status.md of what was fixed, with before/after evidence for the measurable ones (richness-check numbers, hyperui-transplant-check numbers), and be honest about anything that could not be completed in this dispatch (e.g. if hero video rendering genuinely doesn't fit the time budget, say so plainly rather than leaving it silently unfixed).
