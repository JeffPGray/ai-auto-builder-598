Client: cold-front-ac (slug) — Cold Front A/C Of The Woodlands. This is the THIRD dispatch attempt. The first hung (watchdog killed, genuine dead process, unrelated root cause). The second was killed manually by the operator after ~52 minutes on QA round 2 — not a failure, but deemed too slow, because it had loaded the pre-optimization qa-reviewer.md at its own dispatch start and so ran the old fully-serial flow the whole way. Since then, a comprehensive pipeline audit (6 Fable consults) has shipped real fixes across gather/build/QA/deploy, INCLUDING two real bugs caught in the first round of fixes themselves (a wrong file path that silently disabled the Verify/QA dedupe, and a stale session name that could leak a browser) — both now fixed. This dispatch is the first real test of the fully-fixed pipeline.

## Already done — verify intact, do not redo
- `clients/cold-front-ac/data/gathered-content.md`, `data/design-system.md` — real content + design system already captured.
- `clients/cold-front-ac/site/` — full site already built from round 1 (homepage, services index, 4 service pages, about, contact, terms, privacy, blog + 5 articles).
- Round 1 QA (`clients/cold-front-ac/data/qa-report.md`, dated 18:44) found 8 real issues: fabricated homepage reviews (3 invented names/quotes not matching gathered-content.md's 5 real reviews), missing HyperUI usage section in status.md, richness-check FAIL (2 gradients vs 5 planned), 2 mobile-overflow bugs, a wrong Facebook URL, em-dashes in blog prose, and a photo missing a verification verdict.
- A `qa-fix` pass was already applied for some of round 1's issues (page.tsx, globals.css, contact/about pages, HeroVideo.tsx, site-data.ts, blog-data.ts were touched) before this dispatch was killed mid-way through round 2's re-screenshot pass. **Verify what's actually fixed vs still broken** — re-read qa-report.md's 8 items against the current source and confirm each is genuinely resolved, not just attempted. Do NOT assume the prior partial qa-fix pass was complete or correct.

## What to do
1. Confirm the site still builds clean (`cd clients/cold-front-ac/site && npx next build`).
2. Run QA via the `qa-reviewer` agent (per CLAUDE.md's QA Loop) using the now-fixed qa-reviewer.md — this is a genuinely fresh round given real time has passed and the file itself changed, so treat it as a new full round, not a resume of the old round 2. Confirm in your own summary that the Verify/QA dedupe and the parallel screenshot capture actually fire (you should see "carried from Verify" in the gate log if source is unchanged since a Verify pass, and you should see two concurrent playwright-cli sessions doing desktop+mobile work rather than one serial session).
3. If QA reports issues, fix them for real (verify each of round 1's original 8 items specifically — the fabricated-reviews one is the most serious, do not let it slip through again) and re-run QA. Up to 3 rounds max per CLAUDE.md.
4. On PASS: `/deploy cold-front-ac` to the shared PROSPECT lane (`{slug}.grayreserve.agency`) — confirm 200 on the live URL and spot-check the homepage + one service page render correctly.
5. `.env` has `OUTBOUND_SENDER=ghl` + `OUTREACH_ENABLED=true` — do NOT run `/outreach`. `/deploy`'s own mirror tags the contact `demo-built`; the "GR-598 Demo-First Send" workflow owns the send from there. This is a real, live send to coldfrontacofthewoodlands@gmail.com.

## Write the proof
Update `clients/cold-front-ac/data/status.md` with: confirmation each of round 1's 8 issues is genuinely resolved (not just touched), final QA verdict + round count, richness-check and hyperui-transplant-check numbers, deploy live URL + 200 confirmation, GHL contact ID + demo-built tag confirmation, and if queryable, confirmation the GR-598 workflow actually sent the email. Note total wall-clock for THIS dispatch specifically (not the prior two attempts) so we have a real before/after number for tonight's fixes. Be honest about anything that didn't fully complete.
