Build Bluegrass Blast Pressure Washing (slug `bluegrass-blast-pw`) — Georgetown KY pressure washing + soft washing + equipment store.

Phone (502) 603-9849 · email bluegrassblastpw@gmail.com · owner Brad/Bradley Kopp · 177 S Taylor Ln, Georgetown KY 40324.

## Mode

**COMPETE** against their live site https://www.bluegrassblastpw.com/ (site-check `ok`). Pitch is what $598 + $98/mo adds — never "you don't have a website".

**REUSE gather — do NOT re-run `/gather`.** Read `clients/bluegrass-blast-pw/data/gathered-content.md` and `parity-checklist.md` and trust them.

## Experiment: experiment/speed-cut

You are on the speed-cut worktree. Obey:

1. `stages/consult-once.md` — ONE `search.py`, write `design-lock.md`, `node scripts/sync-design-lock.mjs bluegrass-blast-pw`. No Skill-dump of impeccable/anti-ai-slop corpora.
2. Unique BETWEEN sites / coherent INSIDE this site. Do not copy another client's chrome. Do not ship a shared section kit.
3. Stage timers (`stage-timer.mjs`) around copy-template / design / author / blogs / gates / qa-round-N.
4. Sonnet write-once route children (Rule 10 Exception 3). Parent Opus = chrome only.
5. Gates BEFORE screenshots (`pre-qa-gates.sh`). Max **2** QA rounds.
6. Never invent prices beyond gathered $299 / $50 off. Never ship the equine placeholder review. Skip Meta-AI Google photos.

Pipeline: consult-once → design lock + chrome → Sonnet routes → blogs → next build → pre-qa-gates → QA (≤2) → **STOP**.

## HARD STOP — no deploy

⛔ **Do NOT deploy.** Do NOT run `/deploy`. Do NOT touch GHL, outreach, tags, or opportunities. Do NOT register `bluegrass-blast-pw.grayreserve.agency`.

When QA passes (or after 2 rounds), write the verdict into `clients/bluegrass-blast-pw/data/status.md` (DESIGN_IDEA, signature axes, QA rounds, compaction count, stage-timer summary) and exit.

cwd for all work: this app directory. Client dir: `clients/bluegrass-blast-pw`.
