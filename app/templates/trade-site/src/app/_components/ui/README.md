# shadcn mechanics pack (token-restyled)

**Core (required every build):** accordion, dialog, sheet, dropdown-menu  
**Forms:** input, label, textarea, checkbox, radio-group, select, switch  
**Chrome:** tabs, separator, scroll-area, tooltip, popover  

Never add card / button / badge for section composition — CTAs use `.cta-primary`.  
Never ship slate defaults. `cssVariables: false` in components.json.
